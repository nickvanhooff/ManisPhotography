// Temporarily disabled for deployment - will be re-enabled after CMS setup
export default defineEventHandler(async (event) => {
  // Return empty data for now to prevent build issues
  return {
    data: []
  }
})
